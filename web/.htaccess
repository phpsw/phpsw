DirectoryIndex app.php

<IfModule mod_rewrite.c>
    RewriteEngine On

    RewriteRule ^rev/[0-9]+/(.*)$ /$1 [P]

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule .* app.php [QSA,L]
</IfModule>
