<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title>{{ app.title }}</title>
        <meta name="description" content="{{ app.description }}">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="apple-touch-icon" href="/apple-touch-icon.png">
        <link rel="shortcut icon" href="/favicon.ico">
        <link rel="stylesheet" href="/css/fonts.css">
        <link rel="stylesheet" href="/css/style.css">
    </head>
    <body>
        <div class="group">
            <header class="box left">
                <h1>{{ app.name }}</h1>

                <img src="/images/php-elephant.jpg" class="logo" alt="{{ app.name }}">

                {{ meetup.group.description | default | raw }}

                <ul class="list list--icons">
                    <li>
                        <a href="{{ app.meetup.url }}" class="genericon genericon-month">
                            <span>Find us on Meetup</span>
                        </a>
                    </li>
                    <li>
                        <a href="{{ app.twitter.url }}" class="genericon genericon-twitter">
                            <span>Tweet us at @{{ app.twitter.user }}</span>
                        </a>
                    </li>
                    <li>
                        <a href="{{ app.github.url }}" class="genericon genericon-github">
                            <span>Fork this on GitHub</span>
                        </a>
                    </li>
                </ul>

                <div class="box__footer">
                    {% if meetup.group.members is defined %}
                        <a href="{{ app.meetup.url }}/members" class="genericon genericon-user">
                            {{- meetup.group.members }} {{ meetup.group.who | lower -}}
                        </a>
                    {% endif %}

                    {% if meetup.group.rating.average | default > 3 %}
                        <span class="genericon genericon-digg">
                            {{ meetup.group.rating.average | round(1) }}/5 from {{ meetup.group.rating.count }} ratings
                        </span>
                    {% endif %}
                </div>
            </header>

            <div class="right">
                {{ render(path('events')) }}

                {{ render(path('tweets')) }}

                {{ render(path('posts')) }}
            </div>

            <div class="left">
                <section class="contact box">
                    <header class="box__header">
                        <h2>Get in touch</h2>
                    </header>

                    <ul class="list">
                        {% for organiser in app.organisers %}
                            <li>
                                <img src="{{ organiser.photo }}" class="avatar" alt="{{ organiser.name }}">

                                <a href="{{ organiser.url }}">
                                    {{ organiser.name }}
                                </a>

                                <a href="https://twitter.com/{{ organiser.twitter }}">
                                    @{{ organiser.twitter }}
                                </a>

                                {#
                                <span>{{ organiser.email | replace({'@': ' at ', '.': ' dot '}) }}</span>
                                #}
                            </li>
                        {% endfor %}
                    </ul>
                </section>

                {{ render(path('photos')) }}

                {{ render(path('reviews')) }}

                {{ render(path('sponsors')) }}
            </div>
        </div>
        <div id="x" class="lightbox-x"></div>
        <script src="http://code.jquery.com/jquery-2.1.0.min.js"></script>
        <script src="/js/jquery.expander.min.js"></script>
        <script src="/js/jquery.timeago.js"></script>
        <script src="http://maps.google.com/maps/api/js?sensor=true"></script>
        <script src="/js/gmaps.js"></script>
        <script src="/js/main.js"></script>
        <script src="/js/analytics.js"></script>
    </body>
</html>
