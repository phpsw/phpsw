<!DOCTYPE html>
<html>
    <head>
        <title>{{ app.name }}</title>
        <link rel="stylesheet" href="/css/style.css">
    </head>
    <body>
        <div class="group">
            <div>
                <header class="box">
                    <h1>{{ app.name }}</h1>

                    <img src="/images/php-elephant.jpg" alt="{{ app.name }}">

                    <p>{{ app.description }}</p>

                    <ul>
                        <li><a href="{{ app.meetup.url }}">Meetup page</a></li>
                        <li><a href="{{ app.twitter.url }}">Twitter @{{ app.twitter.user }}</a></li>
                    </ul>
                </header>

                <section class="contact box">
                    <h2>Get in touch</h2>

                    <ul>
                        {% for organiser in app.organisers %}
                            <li>
                                <img src="{{ organiser.photo }}" alt="{{ organiser.name }}">

                                <a href="{{ organiser.url }}">
                                    {{ organiser.name }}
                                </a>

                                <a href="https://twitter.com/{{ organiser.twitter }}">
                                    @{{ organiser.twitter }}
                                </a>

                                {#
                                <span>{{ organiser.email | replace({'@': ' at ', '.': ' dot '}) }}</span>
                                #}
                            </li>
                        {% endfor %}
                    </ul>
                </section>

            </div>

            {{ render('/meetup/events') }}

            {{ render('/twitter/tweets') }}

            {{ render('/meetup/posts') }}
        </div>
        <script src="http://code.jquery.com/jquery-2.1.0.min.js"></script>
        <script src="/js/jquery.expander.min.js"></script>
        <script src="http://maps.google.com/maps/api/js?sensor=true"></script>
        <script src="/js/gmaps.js"></script>
        <script src="/js/main.js"></script>
    </body>
</html>
